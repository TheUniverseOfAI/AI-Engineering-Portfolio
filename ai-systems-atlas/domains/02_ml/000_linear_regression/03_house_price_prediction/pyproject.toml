[project]
        name = "002_house_price_prediction"
        version = "0.1.0"
        description = "Regression project"
        requires-python = ">=3.12"
        dependencies = []

        [tool.uv]
        package = false
